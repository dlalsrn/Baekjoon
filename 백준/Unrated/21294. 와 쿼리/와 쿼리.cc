#include <iostream>
using namespace std;

int N = 0, Q, ch;

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cin >> Q;

	for (int i = 0; i < Q; i++)
	{
		cin >> ch;
		if (ch == 1);
		else
			cout << '\n';
	}

	return 0;
}